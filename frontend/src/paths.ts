/** Canonical client routes for profile types (bookmarkable). */
export function criminalProfilePath(id: string) {
  return `/criminal/${id}`;
}

export function entityProfilePath(id: string) {
  return `/entity/${id}`;
}
